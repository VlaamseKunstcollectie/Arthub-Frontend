<% doc_presenter = show_presenter(document) %>

<% if render_iiif_manifest_url(document) == 'n/a' %>
	<div class="image-unavailable">
		<%= image_tag iiif_thumbnail_url_field(document, "600,"), alt: render_image_alt(document) %>
	</div>
<% else %>
  <div class="uv viewer" data-config="/uv-config.json" data-uri="<%= render_iiif_manifest_url(document) %>" data-canvasindex="<%= render_iiif_canvas_index(document) %>"></div>
  <%= PulUvRails::UniversalViewer.script_tag %>
<% end %>

<%# default partial to display solr document fields in catalog show view -%>
<dl class="dl-horizontal  dl-invert">
  <% document_show_fields(document).each do |field_name, field| -%>
    <% if should_render_show_field? document, field %>
	    <dt class="blacklight-<%= field_name.parameterize %>"><%= render_document_show_field_label document, field: field_name %></dt>
	    <dd class="blacklight-<%= field_name.parameterize %>"><%= doc_presenter.field_value field %></dd>
    <% end -%>
  <% end -%>
</dl>
